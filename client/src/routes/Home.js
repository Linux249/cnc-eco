import React from 'react';
import UrlInfo from '../containers/UrlInfo';
import { Row } from '../style/Row';

export default () => {
    return (
        <Row center>
            <UrlInfo />
        </Row>
    );
};
